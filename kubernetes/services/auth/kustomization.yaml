apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - deployment.yaml
  - service.yaml

bases:
  - ../../base

configMapGenerator:
- name: auth-config
  envs:
  - config.env

secretGenerator:
- name: auth-secret
  envs:
  - secret.env