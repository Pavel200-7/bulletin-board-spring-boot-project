apiVersion: v1
kind: Service
metadata:
  name: keycloak-service
  namespace: bulletinboard
spec:
  selector:
    app: keycloak
  ports:
  - port: 8080
    targetPort: 8080
  - port: 9000
    targetPort: 9000