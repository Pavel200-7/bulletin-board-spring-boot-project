apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - deployment.yaml
  - service.yaml

configMapGenerator:
- name: keycloak-config
  envs:
  - config.env

secretGenerator:
- name: keycloak-secret
  envs:
  - secret.env