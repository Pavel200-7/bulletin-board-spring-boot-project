apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - deployment.yaml
  - service.yaml
  - pvc.yaml

configMapGenerator:
- name: rabbitmq-config
  envs:
  - config.env

secretGenerator:
- name: rabbitmq-secret
  envs:
  - secret.env