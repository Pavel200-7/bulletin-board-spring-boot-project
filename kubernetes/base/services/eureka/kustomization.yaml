apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - deployment.yaml
  - service.yaml

bases:
  - ../../base

configMapGenerator:
- name: eureka-config
  envs:
  - config.env