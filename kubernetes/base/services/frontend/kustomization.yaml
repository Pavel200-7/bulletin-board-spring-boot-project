apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - deployment.yaml
  - service.yaml

bases:
  - ../../base

configMapGenerator:
- name: frontend-config
  envs:
  - config.env