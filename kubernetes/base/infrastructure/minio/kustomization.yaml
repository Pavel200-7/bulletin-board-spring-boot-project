apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: bulletinboard

resources:
  - deployment.yaml
  - service.yaml
  - pvc.yaml
  - init-job.yaml 

configMapGenerator:
- name: minio-config
  envs:
  - config.env

